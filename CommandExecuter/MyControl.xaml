<UserControl x:Class="Company.CommandExecuter.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             Name="MyToolWindow" Height="123.068" Width="244.674" Loaded="MyToolWindow_Loaded">
    <Grid>
        <Button Content="Show commands" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="225" Height="44" Click="Button_Click"/>
        <Button Content="Open setting directory" HorizontalAlignment="Left" Margin="85,91,0,0" VerticalAlignment="Top" Width="150" Height="22" Click="Button_Click_1"/>
    </Grid>
</UserControl>
